# Django-management

Criado a partir de aprendizados em curso

# Ambiente Virtual

`python3 -m venv env`
`source env/bin/activate`

# Requirements

`pip install -r requirements.txt`

# Servidor

`./manage.py runserver` 
